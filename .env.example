# Database Connection
# ==================
# For local development, update with your PostgreSQL credentials
# For Prisma Accelerate (production), use the Accelerate connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA

DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/devblog_cms?schema=public"

# Direct Database URL (for Prisma Accelerate)
# Required for migrations and Prisma Studio when using Accelerate
DIRECT_URL="postgresql://johndoe:randompassword@localhost:5432/devblog_cms?schema=public"

# Authentication
# ==============
# Session secret for JWT signing (generate with: openssl rand -base64 32)
SESSION_SECRET="your-secret-key-here-use-openssl-rand-base64-32"

# NextAuth.js (for future implementation)
# NEXTAUTH_SECRET="your-secret-key-here"
# NEXTAUTH_URL="http://localhost:3000"
